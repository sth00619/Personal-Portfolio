{
  "name": "songtaeho-finance-portfolio",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "python3 -m http.server 5174 --bind 127.0.0.1",
    "test": "node --test tests/*.test.mjs"
  }
}
